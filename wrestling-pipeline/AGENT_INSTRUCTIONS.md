Prompt para Configurar Agentes IA en Modo Loop Autónomo

-- Copiado del prompt del usuario: el agente debe seguir el plan aprobado y crear/actualizar archivos en `.agent/`.

Instrucciones de uso:
- Coloca este archivo en la raíz del proyecto.
- Ejecuta `./scripts/start_agent_loop.sh iniciar` para inicializar la carpeta `.agent` y preparar el primer ciclo.
