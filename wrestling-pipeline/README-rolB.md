# Rol B — API + Docker (Estructura y artefactos)

Contenido movido desde la carpeta temporal `mauricio`.

Estructura:

```
wrestling-pipeline/
  ├─ api/
  ├─ etl/
  ├─ docker-compose.yml
  └─ README-rolB.md
```

Instrucciones rápidas:

```bash
cd wrestling-pipeline
docker compose up --build
```

API en `http://localhost:8000` una vez levantado.
